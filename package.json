{
  "name": "app",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20 <23",
    "npm": ">=10"
  },
  "scripts": {
    "start": "cd frontend && npm start",
    "start:api": "cd backend && npm run start:dev",
    "start:api:clean": "cd backend && npm run start:dev:clean",
    "start:all": "concurrently \"npm run start:api\" \"npm run start\"",
    "start:all:clean": "concurrently \"npm run start:api:clean\" \"npm run start\"",
    "build": "cd frontend && npm run build",
    "build:api": "cd backend && npm run build",
    "build:all": "npm run build:api && npm run build",
    "test": "cd frontend && npm test",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
